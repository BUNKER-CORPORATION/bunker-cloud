Add debug visualization to the ChatInterface component by:
1. Adding solid, distinct background colors to all containers (use colors like: pink, blue, green, yellow, purple, orange, red, cyan, teal, indigo, lime)
2. Adding visible labels with black background and white text to each container showing its name
3. Position labels using absolute positioning with appropriate z-index (z-50)
4. Label format: `<div className="absolute top-2 left-2 bg-black text-white px-2 py-1 text-xs z-50 rounded">Container Name</div>`
5. Apply to all major sections: Main Container, Left Panel, Messages Area, Messages Container, Input Container, Input Wrapper, Right Sidebar, Sidebar Content, Close Button Container, Related Resources Section, Related Products Section

Make each container easily identifiable with both color and label so I can reference specific containers for modifications.
